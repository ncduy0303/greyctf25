"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createGetter = void 0;
const params_1 = __importDefault(require("./params"));
const sanitizers_1 = require("./sanitizers");
const createGetter = (basePath) => (target, p, receiver) => {
    if (typeof target[p] === 'function') {
        const func = Reflect.get(target, p, receiver);
        const paramsToSanitize = params_1.default[p];
        if (paramsToSanitize) {
            return (...args) => {
                const sanitizedArgs = args.map((arg, i) => {
                    // the argument could be a file descriptor
                    if (paramsToSanitize.includes(i) && typeof arg !== 'number') {
                        return (0, sanitizers_1.sanitizePath)(arg, basePath);
                    }
                    return arg;
                });
                return func(...sanitizedArgs);
            };
        }
        return func;
    }
    return Reflect.get(target, p, receiver);
};
exports.createGetter = createGetter;
//# sourceMappingURL=getter.js.map